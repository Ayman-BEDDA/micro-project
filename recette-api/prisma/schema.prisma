generator client {
  provider = "prisma-client-js"
  previewFeatures = ["tracing"]
}
datasource db {
  provider = "mysql"
  url      = env("MYSQL_URL")
}
model Recette {
  name        String
  id        Int    @id @default(autoincrement())
  recette        String
  ingredients        String
}